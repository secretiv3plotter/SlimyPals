# Slimy Pals

Frontend offline MVP for raising slime pets with local IndexedDB persistence.

## Scripts

- `npm run dev` starts the Vite dev server.
- `npm run build` creates a production build.
- `npm run lint` runs ESLint.
